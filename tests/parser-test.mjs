/**
 * Quick parser test — verifies that literal `}` characters at the end of
 * snippet bodies are correctly preserved.
 */
import { parseSnippet, snippetPreview } from '../src/features/snippets/parser.ts' assert { type: 'raw' };
